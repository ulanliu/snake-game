{
    "name": "snake-game",
    "version": "1.0.0",
    "description": "Snake Game with React frontend and FastAPI backend",
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix frontend\" \"cd backend && make run\"",
        "dev:frontend": "npm run dev --prefix frontend",
        "dev:backend": "cd backend && make run",
        "test": "concurrently \"npm test --prefix frontend\" \"cd backend && make test\"",
        "test:frontend": "npm test --prefix frontend",
        "test:backend": "cd backend && make test"
    },
    "devDependencies": {
        "concurrently": "^9.1.0"
    }
}